# Precision audit — narrow, high-confidence findings

Tracks a targeted second-pass review running alongside the ongoing
frontend-parity refactor. Scope is deliberately narrow: real correctness/
safety bugs and untested trust boundaries with limited, well-understood
blast radius — not architecture or style cleanup (that's covered by the
existing `plan-v0.1.13.md`/`ARCHITECTURE.md` tracks).

Process: investigate one item at a time, avoid files another agent has open
(check `git status` first), verify with `cargo check`/`clippy -D warnings`
(and tests where the host target allows), commit each landed item
separately, then update this table.

## Findings

| # | Area | File(s) | Status | Notes |
|---|------|---------|--------|-------|
| 1 | fork()/execve() async-signal-safety | `crates/agenterm-platform/src/adapters/linux/pty.rs` (shared by `macos/pty.rs` via `#[path]`) | **Fixed** (`8e1e413`) | Heap allocation (CString/Vec/setenv) after `fork()`, before `execve()`, in a possibly-multithreaded process. Moved all allocation before `fork()`; child branch is now raw libc calls only. Also removed a dead post-fork `setenv` loop that never affected the `envp` already passed to `execve`. |
| 2 | Named-pipe overlapped I/O cancellation | `crates/agenterm-platform/src/adapters/windows/ipc.rs` | **Audited, no bug** | Checked for the classic stack-`OVERLAPPED` use-after-free on timeout (kernel completes cancelled I/O after the stack frame is gone). `wait_overlapped` calls `CancelIoEx` then a *blocking* `GetOverlappedResult` to synchronize before returning — correct. |
| 3 | Cross-process HANDLE duplication | `crates/agenterm-platform/src/adapters/windows/process_reference.rs` | **Audited, no bug** | `RemoteHandleTransfer` uses a rollback-on-drop pattern (duplicates the handle back and closes it in the remote process unless explicitly committed), with a dedicated test (`remote_handle_transfer_rolls_back_until_committed`). Solid. |
| 4 | IPC trust-boundary logic has zero unit tests | `src/control_authority.rs` (313 lines: admit/complete/finish_submission, replay/idempotency) | **Audited, no bug** | The underlying `ReplayWindow` state machine (`control_contract.rs`) is actually thoroughly tested (10+ cases: replay, conflict, expiry, eviction, completion validation, accepted-enrichment, serde roundtrip) — the earlier "zero tests" framing was about the thin `ControlAuthority` orchestration layer only. Traced its one call site (`server_app.rs:1431-1464`): `wait` is derived from the same `response.ok` passed into `complete()`, so the `wait.is_some() => Accepted` override can't fire on a failed response. `admit()`'s identity/fingerprint check recomputes both from the server-received `args` independently of client-supplied `control` metadata, so it's tamper-evident by construction. No bug found; not forcing test-only churn without one. |
| 5 | Reentrant `window_proc` aliases `&mut State` | `crates/agenterm-platform/src/adapters/windows/control_window.rs` | **Fixed** (`20144a1`, refined by `7c7409e`/`7dead17`) | Confirmed reachable chain: `WM_TIMER` → `dispatch` → `RemoteWindowApplication::event` (`src/platform/adapters/windows/remote_frontend.rs:6170`) → `state.tick()` → `process_client_command()` → `execute_client_command()` → a `"window-minimize"`/`"window-maximize"`/`"window-restore"`/`"window-resize"` command → `Backend::set_presentation`/`set_client_size` → `ShowWindow`/`MoveWindow` on the *same* `hwnd`. Win32 delivers the resulting `WM_SIZE`/`WM_WINDOWPOSCHANGED` synchronously, reentering `window_proc` while the outer call still holds `state` live via `&mut *state_ptr` — the reentrant call deref's the same raw pointer into a second, aliasing `&mut State`. This is exactly the hazard `Backend::close()` already defends against (it posts `DEFERRED_CLOSE` instead of calling `DestroyWindow` synchronously) — `set_presentation`/`set_client_size` just never got the same treatment. My initial fix (`20144a1`) added a thread-local `DISPATCHING` guard that dropped reentrant messages to `DefWindowProcW`, correct but lossy (a reentrant `WM_SIZE` would only be reconciled on the next poll tick). A follow-up commit (`7c7409e`, cleaned up by `7dead17`) improved this: reentrant messages are now queued (`PENDING_MESSAGES`) instead of dropped, and replayed through the normal state-dispatch path in a drain-until-empty loop once the outer dispatch has returned and released its `&mut State` — no aliasing (each replay is a fresh, non-overlapping reborrow after the earlier one is out of scope) and no lost messages. `WM_NCDESTROY`'s `GWLP_USERDATA` cleanup stays hoisted above the guard so it always runs regardless of dispatch depth. Re-verified on this Windows host after the follow-up: `cargo check`/`clippy -D warnings` clean. Not verified: an actual end-to-end repro of the reentrant path running the shipped GUI; recommend a manual or CI smoke pass before release. |
| 5b | Highest unsafe density, zero `SAFETY` comments (remaining) | `crates/agenterm-platform/src/adapters/windows/control_window.rs` (93 `unsafe` blocks) | **Audited, no bug; closing** | Covered since item 5: `paint()`'s GDI back-buffer lifecycle, `WinCanvas` pen/brush/font handling, pointer-capture accessors — all solid (see prior note). This pass: the file has no DPI-awareness code and no native `WM_HSCROLL`/`WM_VSCROLL` handling at all (scrollbars are custom-drawn via `ui_geometry`, not native controls), so those categories don't apply here. Checked `run_control_window`'s error/teardown paths: every early-failure branch that calls `DestroyWindow` relies on Win32's guarantee that destroying a parent recursively destroys its children, so partially-created controls/menus never leak. One theoretical (not live) observation: `WM_DESTROY`'s unconditional `PostQuitMessage(0)` would leak a stray `WM_QUIT` to a second window's message loop if `run_control_window` were ever called twice on the same thread after an earlier failure — confirmed via its single call site (`remote_frontend.rs:325`) that this never happens today (one call, whole-process error propagation), so not fixing a scenario that can't occur. Closing this item — further line-by-line unsafe review of this file has hit steeply diminishing returns after four independent passes. |
| 11 | Scrollbar click hit-testing duplicated instead of using `scrollbar_hit_test` | `src/platform/adapters/windows/remote_frontend.rs` (`click_scrollbar`, `click_sidebar_scrollbar`) and `src/platform/adapters/unix/frontend/mod.rs` (`click_sidebar_scrollbar`) | **Fixed** | Found while checking whether the other 9 `#[allow(dead_code)] // Consumed by the Unix frontend adapter` markers in `ui_geometry.rs` were stale like items 9/10's. Three call sites each independently inlined the same `track.contains` → `thumb.contains` → `y < thumb.top` three-way classification instead of calling the existing `scrollbar_hit_test`/`ScrollbarHit` helper that Unix's *terminal* scrollbar click handler already used — not a one-sided Windows-vs-Unix gap this time, both platforms had their own inlined copy for the *sidebar* scrollbar specifically. Converted all three to match on `scrollbar_hit_test`'s result, preserving each site's existing (and differing, correctly) direction-to-action mapping. `cargo check`/`clippy -D warnings`/`cargo fmt --check` clean, `cargo test --lib` (600 tests) passes. |
| 12 | Stale `#[allow(dead_code)]` markers | `src/ui_geometry.rs` | **Fixed** | Audited the 8 remaining `#[allow(dead_code)] // Consumed by the Unix frontend adapter` markers flagged after item 10 (covering `ScrollbarHit`, `WHEEL_DELTA`, `WHEEL_ROWS_PER_NOTCH`, `scrollbar_hit_test`, `terminal_cell_at`, `wheel_delta_units`, `translate_tree_row_geometry`, `sidebar_tree_row_geometry`). All were stale — either already consumed by both frontends or (for `WHEEL_DELTA`/`translate_tree_row_geometry`) only ever used internally within `ui_geometry.rs` itself, where the allow was never necessary. Removed all 8; `cargo check`/`clippy -D warnings` confirm none were actually dead. |
| 13 | Rhai catalog-vs-registration drift spot-check | `src/script_catalog.rs` vs `src/script_net.rs`/`src/script_stdlib.rs` | **Fixed** (`ac14461`, by the other agent) | Follow-up on the original Rhai-runtime review's concern that nothing verifies every catalog entry has a matching registered native fn. I manually spot-checked `std.net.*`/`std.fs.*` (clean) and identified the real blocker for automating it: `Engine::gen_fn_signatures()` is exactly the right API but sits behind rhai's `metadata` Cargo feature, which this repo doesn't enable in `[dependencies]` because of the release binary-size budget. **Resolved by the other agent, reading this entry**: enabled `metadata` only under `[dev-dependencies]` (`Cargo.toml`) — available to `cargo test` via feature unification, never compiled into release binaries, sidestepping the size-budget conflict entirely without needing the fiddlier hash-based alternative I'd proposed. `script_stdlib::register_local` now delegates to a `build_local_modules()` that returns the actual registered module tree, and a new test (`every_shipped_plain_api_resolves_in_registered_surface`) walks that tree checking every shipped non-Fleet catalog entry's exact module path and arity, with 28 type-method/property/CLI/import entries explicitly triaged as non-directly-callable rather than silently skipped. Verified: `cargo test --lib script_catalog::` (9 tests) and the full 602-test suite both pass. |
| 14 | Unix-socket IPC trust boundary audit | `crates/agenterm-platform/src/adapters/linux/ipc.rs` | **Audited, no bug** | Thorough security-focused pass over `NativeListener::bind`, `verify_peer_uid`, `ensure_private_directory`, and `UnixInstanceLease`. All correct and unusually well-defended: `verify_peer_uid` uses kernel-enforced `SO_PEERCRED`/`getpeereid` (not spoofable by the client); `ensure_private_directory` walks every path ancestor rejecting symlinks/non-directories before creating with mode 0700, then re-verifies after an `AlreadyExists` race; stale-socket recovery checks device+inode identity didn't change between the initial stat and the removal (TOCTOU-safe) and requires a valid predecessor PID/start-time lease before ever unlinking; `UnixInstanceLease::acquire` uses `O_CREAT\|O_EXCL\|O_NOFOLLOW` for atomic, symlink-proof lock creation plus `flock`, and its `Drop` re-checks dev/inode/uid before removing the lock file so it can never delete a different process's file. No further action needed. |
| 15 | Shell-injection-shaped clipboard helper probe | `crates/agenterm-platform/src/adapters/linux/clipboard.rs` (`command_exists`) | **Fixed** | `command_exists` ran `sh -c "command -v {program} >/dev/null 2>&1"` with `program` interpolated directly into the script string via `format!`. Not currently exploitable — every call site passes a hardcoded literal (`"wl-copy"`, `"wl-paste"`, `"xclip"`, `"xsel"`) — but empirically confirmed the *pattern* was genuinely exploitable (a `program` value like `"ls; touch /tmp/marker"` executed the injected command) and would become live the moment any caller ever derived the program name from configuration. Fixed by passing `program` as a positional shell parameter (`sh -c 'command -v "$1" ...' sh program`) instead of interpolating it into the script text — empirically re-verified the injection no longer executes while real binaries are still correctly detected. `cargo check`/`clippy -D warnings` clean on the Linux cross-target. |
| 16 | Instance-registry write/lock audit | `src/instances.rs` | **Fixed (1 of 2 observations); 1 remains open** | Swept for the same "sh -c"/shell-construction shape that produced item 15 across the whole repo — no other instance found (the fix was the only one). Separately audited `instances.rs`'s registration lifecycle: `Drop for InstanceRegistration` re-verifies pid/address/endpoint/scope/process-start-identity/lease-nonce/epoch before deleting (same correct verify-before-delete pattern as the Linux IPC lease), and `discover_instances_in` gracefully skips unreadable/malformed entries rather than erroring the whole scan. **Observation (1) resolved**: `register_instance_in`'s `if path.exists() { remove_file }` before `fs::rename` was flagged as possibly-redundant but left alone pending confidence. Built a standalone probe binary and empirically tested `fs::rename` on this Windows host across three scenarios — no open handle, destination held open via `fs::File::open`'s default sharing flags, and explicit `remove_file` while open — all three succeeded cleanly (Windows's `MOVEFILE_REPLACE_EXISTING` behavior applies, and Rust's default `File::open` sharing flags include `FILE_SHARE_DELETE`). Confirmed the pre-removal protects against nothing `fs::rename` doesn't already handle, and only narrows the atomicity window (a concurrent reader could observe the path as transiently missing). Removed it. `cargo check`/`clippy -D warnings` clean, all 11 `instances::` tests plus the full 601-test suite pass. **Observation (2), now fully investigated rather than left as a guess**: traced `instances_dir()` → `paths::instance_registry_dir` → `platform::instance_registry_directory_root()` → `host_directories().local_data`. In the common case this resolves to a genuinely per-user path (`%LOCALAPPDATA%` on Windows, `$XDG_DATA_HOME`/`~/.local/share` on Linux/macOS) where the symlink-hijack threat model doesn't really apply (another user would need write access to your home directory, at which point they already own your session by far more direct means) — so the original "likely fine" instinct was right for the happy path. But `host_directories()` *can* fail — on Linux/macOS specifically when both `HOME` and `XDG_DATA_HOME`/`XDG_CONFIG_HOME` are unset (`adapters/linux/filesystem.rs:24-38`) — and `src/platform/policy/paths.rs:69-77`'s `host_directories()` wrapper silently falls back to the *shared* `std::env::temp_dir()` (`/tmp` on Unix) with **no** symlink/ancestor hardening applied, unlike `linux/ipc.rs::ensure_private_directory`'s handling of the structurally identical problem for the IPC socket path. In that fallback state, a co-resident user on a shared Unix host could pre-plant a symlink at the resolved instances-directory path, and `register_instance_in`'s `fs::create_dir_all`/`fs::write`/`fs::rename` would follow it — a real, if narrow (requires both `HOME` and every `XDG_*` var unset, an unusual environment), file-planting primitive. Windows is meaningfully less exposed here since `%TEMP%` is per-user by OS convention even when `LOCALAPPDATA` specifically is unset. Not fixing this now: the correct shape is reusing/extracting an `ensure_private_directory`-equivalent check for this fallback path — `crate::filesystem_entry::metadata_is_real_directory` plus the existing `protect_private_directory` helper (`adapters/windows/filesystem.rs:95`) look like reusable building blocks already in the codebase — but doing it with the same care the original ancestor-walk got deserves more than one loop increment; noted precisely for whoever picks it up next. |
| 6 | Win32 clipboard ownership transfer | `crates/agenterm-platform/src/adapters/windows/clipboard.rs` | **Audited, no bug** | Checked against AGENTS.md's exact spec (movable NUL-terminated UTF-16, ownership transferred only after `SetClipboardData` succeeds, `GlobalFree` on every pre-transfer failure). `set_text_with_owner` matches it precisely, including the easy-to-get-wrong byte-vs-code-unit size passed to `GlobalAlloc` (`mem::size_of_val(encoded.as_slice())`, correctly `len * 2`, not `len`). `get_text` never frees the `GetClipboardData` handle (correct — the system owns it) and bounds the read size before locking. |
| 7 | AppContainer / process containment unsafe blocks | `crates/agenterm-platform/src/adapters/windows/{app_container,process_security,process_containment}.rs` | **Audited, no bug** | `process_security.rs`'s `copy_sid` explicitly bounds-checks that a token-supplied SID pointer actually lies within the queried buffer before copying it out — a real defense against a SID pointer landing somewhere unexpected, not just a happy-path copy. `app_container.rs`'s `OwnedAppContainerSid`/`AppContainerCapabilitySid` pair every `FreeSid`/`LocalFree` correctly: freed exactly once on every validation-failure path (no leak), never touched again after (no double-free), and `create_profile`/`derive_profile_sid` immediately hand the raw output SID to `OwnedAppContainerSid::from_raw` so there's no window where a native allocation is held as a bare pointer. `process_containment.rs` (previously deferred): `create()`'s `SetLastError(0)`-before/`ERROR_ALREADY_EXISTS`-after pattern for `CreateJobObjectW` is the correct idiom, and the handle is still RAII-dropped (closed) on that path since it's never moved into `Self` before the early return; `process_ids()`'s growable-buffer retry loop (`usize`-granularity allocation for correct alignment, byte-arithmetic for the variable-length `ProcessIdList` array, doubling-or-`NumberOfAssignedProcesses` regrowth on `ERROR_MORE_DATA`) checks out against the documented Win32 contract for `JOBOBJECT_BASIC_PROCESS_ID_LIST`. Follow-up landed: added `process_ids_grows_the_buffer_past_the_initial_capacity`, assigning 20 concurrently-alive processes to force the `ERROR_MORE_DATA` regrowth branch. First run caught a real bug — in the *test*, not the production code: each `cmd.exe /c "ping ..."` child also spawns a `ping.exe` descendant that automatically inherits job membership on Windows, so the job ends up with roughly 2x the directly-assigned pids (37 observed vs. 20 expected) — asserting exact set equality failed. Fixed the assertion to check `assigned.is_subset(&observed)` plus `observed.len() > 16` instead, which is what the test actually needs to prove (buffer grew past capacity, and every process we know we assigned is present) without assuming Windows job-object descendant-inheritance details. `cargo check`/`clippy -D warnings`/`fmt --check` clean, full `agenterm-platform` suite now 224 tests (was 223), all passing. |
| 8 | Unbounded per-stream drain thread spawn | `src/script_stream.rs` (`from_reader_inner`) | **Fixed** (`b6b9e5c`) | Implemented the recommended design (option A) proposed in this doc: a global `StreamPumpPermit` (cap 64, `AtomicUsize` CAS acquire, `Drop`-released) acquired before spawning a pump thread; `from_reader`/`from_bounded_reader`/`from_process_stdout`/`from_process_stderr` now return `Result` and fail with a typed `stream_pump_limit` error instead of spawning past the cap. Chose typed-error over blocking backpressure to keep the change small and fail fast rather than add starvation-prevention machinery for scripts that hold streams unread. Updated the 3 real call sites: `script_process.rs`'s stdout/stderr now kill+reap the already-spawned child if stream creation is denied (so a denied permit can't leak a live process), `script_http.rs` maps the typed error into its `String` error type. `cargo check`/`clippy -D warnings` clean, `cargo test --lib` (601 tests) passes. Not added: a dedicated test that exhausts the real 64-permit limit — the counter is a shared global `static`, and cargo runs this file's tests in parallel in one process, so deliberately holding 64 permits in one test risked flaking the other stream tests that run concurrently in the same binary; relying on code review of the straightforward CAS loop plus the existing (now `Result`-returning) tests exercising normal operation instead. |
| 9 | `close_tab` diverged active-tab reassignment | `src/server_app.rs:1585-1630` vs `src/platform/adapters/unix/frontend/mod.rs` (`close_tab_id`) | **Fixed** | Re-checked once the other agent's in-flight edits to this file landed (working tree went clean). The two implementations still aren't shared code, but the concrete behavioral bug is fixed: `close_tab_id` picked the new active tab via `self.tabs.first()` (always jumps to the top of the list) while `server_app.rs`'s `close_tab` uses `self.tabs.get(position).or_else(|| position.checked_sub(1)...)` (keeps the closest surviving neighbor). Aligned `close_tab_id` to the same neighbor-preserving logic. `cargo check`/`clippy -D warnings` clean, full `cargo test --lib` (593 tests) passes. Did not merge the two functions into one shared implementation — that's a larger refactor better left to whoever owns the ongoing frontend-parity consolidation, to avoid colliding with that work. |
| 10 | Sidebar geometry math duplicated (Windows reimplements the shared fn) | `src/ui_geometry.rs:686,803` vs `src/platform/adapters/windows/remote_frontend.rs` | **Fixed** | Confirmed byte-for-byte identical formulas (`sidebar_row_capacity`, and the thumb-height/travel/thumb-top math in `sidebar_scrollbar_state`) between the two files — the Unix frontend already called the shared `ui_geometry` functions the same way. Switched Windows's two reimplementations to call the shared functions instead (imported `sidebar_row_capacity`/`sidebar_scrollbar_geometry`, dropped the now-unused `TAB_HEIGHT`/`TAB_TOP` imports it no longer needs directly). Removed the two functions' `#[allow(dead_code)] // Consumed by the Unix frontend adapter` markers since they're genuinely used by both frontends now and no dead-code warning fires without the allow. `cargo check`/`clippy -D warnings`/`cargo fmt --check` clean, full `cargo test --lib` (593 tests) passes. Noted but not fixed: 9 other `#[allow(dead_code)] // Consumed by the Unix frontend adapter` markers remain in `ui_geometry.rs` (lines 441-726) — didn't audit whether they're similarly stale now; out of scope for this specific finding. |
| 17 | Worker concurrency-limiting machinery audit | `src/worker_supervisor/mod.rs`, `src/platform/services/supervisor_audit.rs`, `crates/agenterm-platform/src/adapters/windows/locking.rs` (`SlotPermit`) | **Audited, no bug** | Checked the two-tier permit item 8 leaned on without independently verifying: `ConcurrencyPermit::try_acquire` correctly rolls back its in-process `AtomicUsize` increment (`fetch_sub`) whenever the per-process check fails *or* the cross-process `SlotPermit` acquire fails — no leaked count on any rejection path. `SlotPermit` (Windows) enforces the cross-process global cap via one named `CreateMutexW` per slot number (deterministic identity from a 64-bit fingerprint of `directory:namespace`), trying slots `0..limit` until one is free; `WaitForSingleObject`'s `WAIT_ABANDONED` (previous holder crashed without releasing) is correctly treated as a successful acquisition rather than an error, so a crashed worker can't permanently strand a slot. Every failure path (`CreateMutexW` returning null, `WaitForSingleObject` timeout/failure) closes the handle before returning — no handle leak. In-process double-reservation is guarded by a separate `Mutex<HashSet<String>>` with poison-recovery (`unwrap_or_else(PoisonError::into_inner)`) rather than panicking. No bug found. |
| 18 | Task concurrency cap audit — confirms item 8's precedent | `src/script_task.rs` | **Audited, no bug** | While tracing how `http.start()` (the async/task path) reaches `perform_request`, found `script_task.rs` already had the *exact same* pattern I built for item 8 — `MAX_ACTIVE_TASKS = 64`, an `AtomicUsize` counter, an `ActiveTaskPermit` with `Drop`-release, acquired via `fetch_update` (a cleaner built-in than the manual CAS loop I wrote for `StreamPumpPermit` — style note, not a bug) before every `thread::spawn`. Confirms two independent caps stack for `http.start()`: the pre-existing 64-task cap plus my new 64-stream-pump cap; `http.request()` (synchronous, no task thread) only ever had the second one, which is exactly the gap item 8 closed — so the fix was additive, not redundant. Also checked `cancel_inner`/`wait_inner`'s condvar interaction: cancelling a pending task while its timer thread is blocked in `wait_timeout_while` correctly wakes and re-checks the predicate, so the timer thread can't race past a cancellation and overwrite `Cancelled` back to `Completed`. `race()` polls every 1ms across all raced tasks rather than a single condvar wait (can't wait on N condvars at once in std Rust) — a minor inefficiency, not a correctness bug. No issue found. |
| 19 | First macOS-specific audit pass | `crates/agenterm-platform/src/adapters/macos/{process_window,clipboard}.rs` | **Audited, no bug** | macOS platform code hadn't been touched this whole review; confirmed `cargo check -p agenterm-platform --target x86_64-apple-darwin` works from this Windows host for type-checking (no linker needed), same as the Linux cross-check used for items 1/15. `clipboard.rs` shells out to `pbcopy`/`pbpaste` via plain `Command::new(program)` — no `sh -c` involved at all, so the injection shape fixed in item 15 doesn't exist here. `process_window.rs` (CoreFoundation-based window discovery/key-event synthesis for `new-agent`-style automation): `OwnedCf`'s `CFRelease`-on-drop wrapper is only ever constructed from documented Create/Copy-rule APIs (`CGWindowListCopyWindowInfo`, `CGEventSourceCreate`, `CGEventCreateKeyboardEvent`) — the classic CF bug is over-releasing a borrowed "Get"-rule reference, and `CFArrayGetValueAtIndex`'s per-element results are correctly left unwrapped (not owned) rather than passed through `OwnedCf`. `dictionary_i32` checks `CFGetTypeID(value) == CFNumberGetTypeID()` before calling `CFNumberGetValue`, guarding against a dictionary entry not being the expected type. `resolve_candidates`/`required_candidate`'s 0/1/many slice-pattern match cleanly handles missing/unique/ambiguous window search results. No bug found. |
| 20 | macOS process enumeration audit | `crates/agenterm-platform/src/adapters/macos/process.rs` (`list`) | **Audited, no bug** | Checked specifically for the item-1 fork()/exec class of bug (macOS process spawning goes through `std::process::Command`, not a hand-rolled fork, so that risk doesn't apply here — Rust's std already handles the async-signal-safety concern upstream). `list()`'s two-call `proc_listpids` pattern (query required size, then snapshot) adds a 32-slot headroom margin against the list growing between the two calls, uses `try_from` with typed overflow errors rather than truncating casts for the buffer-size conversion, and correctly `truncate()`s to the *actual* bytes returned rather than assuming the buffer was fully populated. The secondary `proc_pidinfo` parent-PID lookup checks the returned byte count equals the exact expected struct size before trusting the output, falling back to 0 rather than reading a partially-filled struct. No bug found. |
| 21 | Proxy-credential stream redaction audit | `src/terminal_runtime.rs` (`redact_proxy_stream_chunk`) | **Audited, no bug** | Checked the streaming redaction of proxy credentials from live terminal output for the class of bug I expected: a secret split across two PTY read chunks slipping through unredacted. It's handled correctly — needles are sorted longest-first (so a shorter needle that happens to be a prefix of a longer one can't shadow the correct match), and when not flushing, the scanner explicitly `break`s early whenever the remaining buffered bytes are a strict prefix of some needle longer than what's been seen so far, deferring the decision to the next chunk instead of guessing. Confirmed via existing tests that already exercise exactly this split-across-calls scenario. `needles` are re-derived from `self.proxy_redaction_needles` fresh on every call (via `SecretValue::expose_bytes`), so a credential rotation can't leave a stale needle in effect. Also checked `resize_after_native_accepts`: it only commits the tracked size and screen state *after* the native resize call succeeds, so a failed resize can't leave the parser and the tracked size disagreeing — backed by a dedicated test. No bug found. |
| 22 | Unbounded frame/invocation dedup sets in the persistent worker | `src/script_protocol.rs` (`ScriptFrameTracker`), `src/bin/agenterm-rhai.rs` (`process_concurrent_framed_worker`, `completed`) | Found, needs a design decision before fixing | While auditing frame parsing (`read_script_frame` correctly bounds the length prefix before allocating, and drains a rejected oversized frame's claimed bytes to keep the stream in sync — solid), noticed `ScriptFrameTracker`'s two `HashSet<String>` fields (`seen_frames`, `known_invocations`) and `process_concurrent_framed_worker`'s separate `completed: HashSet<String>` are all inserted-into on every frame/invocation but **never pruned**, for as long as the worker process lives. Confirmed this is reachable, not theoretical: `worker_supervisor/persistent.rs`'s own doc comment states the persistent worker "never restarts itself" except on an explicit crash/hard-interrupt "fresh session" — there's no routine recycling by invocation count or time — so a long healthy interactive REPL session accumulates all three sets indefinitely. Per-entry cost is small (frame/invocation IDs are capped at 128 bytes plus `HashSet` overhead), so this is a slow-building concern, not an acute one, but it is a real, unbounded memory growth in a process explicitly designed to be long-lived. Notably, `ReplayWindow` (`control_contract.rs`, audited in item 4) already solves the *same* class of problem correctly — bounded capacity, evicting only entries that have reached a terminal/completed state — and none of these three sets got the same treatment. **Not implementing without a steer**: bounding these sets changes replay/cancel-classification semantics (an evicted old frame/invocation ID could then be legitimately reused, weakening duplicate-detection for very old IDs) — the same category of product decision as item 8's concurrency limit, not something implied by the existing code. |
| 23 | Control-Center webview bridge validation audit | `src/webview_host.rs` (`BridgeSession::begin`, `BridgePermit`) | **Audited, no bug** | Checked the embedded-webview message-bridge trust boundary — origin/frame/size/JSON/version/nonce/request-id/method-allowlist/empty-params/deadline/concurrency checks, in that order. Origin check is exact string equality against a hardcoded constant (no prefix/substring bypass surface). Deadline validation bounds both directions (rejects already-expired *and* implausibly-far-future deadlines, closing a resource-hold vector). `BridgePermit`'s `Drop` releases the concurrency counter via the same acquire-with-rollback-on-rejection-plus-`Drop`-release idiom now confirmed correct in four other places this session (`ConcurrencyPermit`, `StreamPumpPermit`, `ActiveTaskPermit`, `BridgePermit`). No bug found. |
| 24 | MCP sidecar active-wait map audit — contrast with item 22 | `src/mcp_stdio.rs` (`active: HashMap`) | **Audited, no bug** | Went looking for more instances of item 22's "insert but never prune" pattern across the codebase; this one is the counterexample done right. `active` tracks in-flight `wait` requests, bounded by an explicit `waiter_concurrency` cap at insertion, and unconditionally `remove()`d on `WaitComplete` before branching on success/error — so both outcomes clean up, and the map can never grow past its configured concurrency limit regardless of how long the sidecar runs. Confirms item 22 is a real, specific gap in three particular sets, not a systemic pattern across the codebase. |
| 25 | MCP `agenterm_wait` polling loop audit | `src/mcp_fleet.rs` (`wait_event`) | **Audited, no bug** | Checked the polling loop backing the `agenterm_wait` MCP tool. `after` (the event sequence cursor) is monotonically advanced via `after.max(sequence)` from both matched events and the batch's trailing position, so it can never regress even if events arrived out of order. `server_restart`/`journal_gap`/`future_sequence` backend error codes are surfaced as typed outcomes to the MCP caller rather than causing a crash or a silent retry-forever loop — correct, since a client-requested `--after` beyond the server's current sequence is a client-side bug to report, not something to wait out. The inter-poll sleep is capped by the remaining deadline so it can't oversleep past a timeout. `ensure_resource_items`/`ensure_resource_bytes` bound both item count and encoded byte size against configured limits before ever returning a resource. No bug found. |
| 26 | STILL_ACTIVE ambiguity in process liveness check | `crates/agenterm-platform/src/adapters/windows/process_observation.rs` (`observe`) | **Fixed** (`4a8b5d3`) | `observe()` compared `GetExitCodeProcess`'s output against `STILL_ACTIVE` (259) to decide whether a process is still running — a well-documented Windows ambiguity: a process that legitimately exits with code 259 is indistinguishable from one still running by that check alone. Load-bearing: `instances.rs::instance_process_is_alive` uses this to decide whether a registered instance's owning process is still alive (feeding stale-registration cleanup), and `script_process.rs` uses the same contract type for Rhai child-process tracking. Replaced the exit-code heuristic with `WaitForSingleObject(process, 0)`, which reports the handle's actual signaled state unambiguously; `GetExitCodeProcess` is no longer needed for liveness at all. **My first attempt at this fix had its own bug**, caught only because I wrote a real test instead of trusting the type-check: `WaitForSingleObject` needs `SYNCHRONIZE` access, which `PROCESS_QUERY_LIMITED_INFORMATION` alone doesn't grant, so the first version returned `Unknown` for every process. Fixed by requesting `PROCESS_QUERY_LIMITED_INFORMATION \| SYNCHRONIZE_ACCESS`, matching the access mask `process_reference.rs` in this same crate already uses correctly for the identical pattern. Added a test that spawns a real child exiting with code 259 and confirms it's reported `Dead` — this failed against both the original code and my first (SYNCHRONIZE-missing) attempt, and passes against the final fix. `cargo check`/`clippy -D warnings` clean, full `agenterm-platform` suite (225 tests) passes. Landed carefully alongside the other agent's concurrent in-flight `Cargo.toml`/`Cargo.lock`/`script_catalog.rs` changes — staged and committed only this one file. |
| 27 | Last-error captured after an intervening Win32 call | `crates/agenterm-platform/src/adapters/windows/process_metrics.rs` (`metrics`) | **Fixed** (`30650fd`) | `metrics()` called `GetProcessTimes` then `K32GetProcessMemoryInfo`, and only read `std::io::Error::last_os_error()` afterward, gated on either call failing. If `GetProcessTimes` failed but `K32GetProcessMemoryInfo` then succeeded, the captured error would reflect whatever the thread's last-error happened to be after the *second*, successful call — Win32's last-error is only reliably meaningful immediately after the specific call being inspected, not after an intervening call. Low severity: the function still correctly returned `Err` either way, this only risked attaching the wrong OS error code/message to it. Fixed by capturing each call's error immediately after it runs, before the next Win32 call can touch the thread's last-error state, then taking whichever one fired. `cargo check`/`clippy -D warnings` clean, full `agenterm-platform` suite (225 tests) passes. Again landed alongside the other agent's concurrent `Cargo.toml`/`Cargo.lock`/`script_catalog.rs` work without touching those files. |
| 28 | Sweep for more last-error-ordering bugs (item 27's class) | `crates/agenterm-platform/src/adapters/windows/*.rs` (~25 files using `last_os_error`/`GetLastError`) | **Swept, no other instance found** | Since item 27 was a real bug, checked whether "capture the error after a later, possibly-clobbering call" is a systemic pattern elsewhere. Spot-checked the highest-call-count files (`process_spawn.rs`, `app_container_process.rs`, `user_identity.rs`, plus re-confirming `process_reference.rs`/`process_security.rs`/`ipc.rs` from earlier passes): every other site captures `last_os_error()`/`GetLastError()` immediately after its own specific call, before any other Win32 call can run. Item 27 was an isolated instance (the only place two independent Win32 calls shared one deferred error read), not a codebase-wide habit. |
| 29 | Tab-environment CLI arg parser audit | `src/commands.rs` (`parse_tab_environment`, `validate_environment_name`/`_value`) | **Audited, no bug** | `validate_environment_name` correctly restricts child-process env var names to POSIX-identifier shape (alnum/underscore, no leading digit) and reserves the `AGENTERM_` prefix so a tab-scoped `-e`/`--env` override can't spoof AgenTerm's own injected context variables; `validate_environment_value` rejects embedded NULs (the one thing that would actually corrupt a C-style env value). Noticed `parse_tab_environment`'s scan silently tolerates a value-taking flag (`-t`, `-n`, etc.) appearing last with no following value, just letting the position counter run past the end and the loop terminate — traced this to `option_value()` (used everywhere these flags are actually consumed), which already returns `None` gracefully for a missing value rather than panicking, and callers already treat `None` as "use the default." Confirmed this is consistent, intentional permissive-parsing style throughout the CLI layer, not an overlooked validation gap. |
| 30 | tmux-compatible key-encoding audit | `src/commands.rs` (`tmux_key_bytes`, `tmux_key_bytes_with_modifiers`, `xterm_modifier_code`) | **Audited, no bug (one documented scope gap noted)** | Verified the `C-<letter>` control-key formula (`(upper as u8) - b'@'`) against the standard control-character encoding — correct for A-Z (e.g. `C-c` → 0x03, matches the existing test). `xterm_modifier_code`'s `1 + shift(1) + alt(2) + ctrl(4)` bit-weighting matches the documented xterm CSI modifier-parameter convention, and correctly returns `None` for "no modifier" so callers fall back to the unmodified sequence instead of emitting a redundant `;1`. One thing noticed but not a bug to fix: `C-<letter>` only handles `is_ascii_alphabetic()`, so common combos like `C-[` (Escape), `` C-\ ``, `C-]` all fall through to `None` (unsupported) rather than encoding the real control codes for those punctuation keys. No existing test claims coverage for this either. Given AGENTS.md's explicit "do not claim full tmux/RMUX compatibility... unsupported commands must fail explicitly" philosophy, returning `None` here already *is* the documented correct behavior for an out-of-scope binding, not an overlooked gap — expanding it would be a scope decision, not a bug fix. |
| 31 | Non-atomic settings.json write | `src/settings.rs` (`save_config`) | **Fixed** (`84ce5ff`) | `save_config` wrote directly to the final `settings.json` path via `std::fs::write`. A crash or power loss mid-write would leave the file truncated, and `load_config` silently falls back to `AppConfig::default()` on a parse failure — discarding every user preference (font, theme, tab width, all per-tab appearance overrides) with no warning. `instances.rs` already uses the correct co-located-temp-file-then-`rename` pattern for its own persistence (see items 16/31 for the same idiom); `settings.rs` was the inconsistent one, for arguably more user-visible data. Applied the same pattern. `cargo check`/`clippy -D warnings` clean, all 16 `settings::` tests plus the relevant `frontend::settings` tests pass. Did not add a dedicated round-trip test for the temp-then-rename mechanics itself: `config_path()` supports an `AGENTERM_SETTINGS_PATH` env var override for testability, but safely mutating process-wide env state across this codebase's parallel test suite needs the same `PROCESS_TEST_LOCK`-style serialization `worker_supervisor::tests` already uses for the identical reason — noted as a follow-up rather than risking new flakiness. |
| 32 | Non-atomic workspace.json write | `src/workspace.rs` (`save_workspace`) | **Fixed** (`2964650`) | Same bug class as item 31, found by grepping for other direct `fs::write(&path, ...)` sites after fixing settings.rs: `save_workspace` wrote the whole tab tree straight to `workspace.json` with no atomicity, and `load_workspace` treats a parse failure identically to "no saved workspace" — a crash mid-write would silently discard every tab's name, note, composer draft, and command line on next launch. This directly threatens the remain-on-exit / workspace-restoration invariant AGENTS.md documents as a core guarantee, arguably higher-stakes than the settings fix. Applied the same co-located-temp-file-then-rename pattern. `cargo check`/`clippy -D warnings` clean, full 602-test suite passes. Landed cleanly alongside the other agent's large in-flight console-line-editor feature (many staged files) — caught the same staged-file collision risk as before `git commit` this time (via `git reset` before re-staging), not after. Swept every other `fs::write(&path, ...)` site in the repo while here: `control_center.rs`'s and `instances.rs`'s hits are test fixtures deliberately writing corrupt/incompatible data to test error handling, `script_stdlib.rs`'s is the Rhai `std.fs.write` primitive itself (intentionally mirrors `std::fs::write`'s own non-atomic semantics — a script asking to write a file shouldn't get surprise atomicity it didn't ask for), and `process_spawn.rs`'s is a test-only PID probe file. `settings.rs`/`workspace.rs` were the only two real production persistence gaps, and both are now fixed. |
| 33 | Script audit log persistence audit | `src/script_audit/mod.rs` | **Audited, no bug** | Checked while sweeping for more instances of items 31/32's bug class. This one is append-only (`OpenOptions::new().create(true).append(true)`), a fundamentally different and appropriate pattern for a log versus a state snapshot — a crash mid-append at worst truncates the newest record, it can't corrupt records already flushed, so the whole-file-rewrite atomicity concern doesn't apply. Rotation (remove old backup, rename active file to backup, let the next open recreate it) has no data-loss window either: a crash between the rename and reopen just means the next write recreates an empty active file, losing nothing already flushed to the now-renamed backup. No bug found. |
| 34 | Shell-quoting empirical verification | `src/working_context.rs` (`quote_for_bash`, `quote_for_powershell`, cmd.exe path in `shell_command_for_child`) | **Verified correct, empirically** | This is CWD-editor/proxy-command shell quoting — a security-critical boundary I'd only seen praised secondhand (existing test names) earlier this session, never personally verified. Compiled the exact two quoting functions from the real source into a standalone binary, generated their output against adversarial payloads containing embedded single quotes plus shell metacharacters (`; touch ...`, `; Set-Content ...`), then *actually executed* the resulting quoted strings in real `bash` and real `powershell` on this host — both times the payload was echoed back literally with no command execution, confirming the quote-and-reopen escaping (`'\''`  for bash, `''` for PowerShell) is correct, not just plausible-looking. The cmd.exe path takes a different, appropriately conservative approach: it rejects any command/argument containing `"`/`%`/`!` outright rather than attempting to quote them (cmd.exe's expansion rules make robust escaping of those characters genuinely hard to get right) — refusing rather than guessing is the correct call there. No bug found; this is a "checked and it's solid" confirmation, done to the same standard as items 15's exploit-and-fix rather than a read-through. |
| 35 | OSC7 CWD reporting: untrusted-input parsing pipeline | `src/working_context.rs` (`parse_osc7`, `parse_file_uri`, `percent_decode`, `valid_display_path`, `validate_path`, `cwd_command`) | **Audited, no bug** | OSC7 is genuinely adversarial input — any program running inside the PTY (including a compromised or malicious one) can emit it, and it feeds into a CWD string that later gets built into a real shell command. Checked the full pipeline: raw URI bytes are rejected if non-ASCII *before* percent-decoding (so non-ASCII paths must arrive correctly percent-encoded, not as raw UTF-8 claiming to be ASCII); the authority/host component is restricted to alnum+`.-_` and must be empty/`localhost`/the known local hostname, rejecting a spoofed remote-host CWD claim; `percent_decode` is bounds-checked against a truncated trailing `%XX` (no panic) and rejects any decoded output containing an ASCII control byte, including NUL; the Windows drive-letter unwrap (`/C:/path` → `C:\path`) only fires on the exact `/<letter>:` shape. Then `cwd_command` re-validates independently per shell and uses the same escaping already verified correct in item 34 for PowerShell/Bash, plus PowerShell's `-LiteralPath` (correctly disables glob expansion for a literal path) and Bash's `cd --` (defends against a path starting with `-` being read as an option). Defense is layered, not single-point: `valid_display_path` and `validate_path` duplicate the empty/length/control-character checks at different call sites rather than trusting one gate. No bug found. |
| 36 | Directory-publish (release-staging) rename-with-rollback primitive | `crates/agenterm-platform/src/filesystem_publish.rs` (`publish_directory`, `validate`, `publish_with`, `unique_backup`) | **Audited, no bug** | High-stakes (release artifact staging) directory-swap primitive, and one of the most carefully engineered pieces of persistence code found this session. `validate()` requires staging/destination to be distinct, share one canonicalized (symlink-resolved) physical parent directory (so the swap's renames stay same-filesystem and thus atomic), rejects a symlink passed as staging, and — if a destination already exists — resolves both sides' canonical identity to reject staging/destination being aliases of the same directory via different paths. `publish_with()` implements a proper two-phase swap: rename existing destination to a unique backup, rename staging into place, and on that second rename's *failure* attempts to roll back by renaming the backup back — if the rollback itself fails, it returns a distinct typed error carrying the backup path rather than silently losing track of it. Post-success backup cleanup failure is correctly reported as a separate, non-fatal signal (`retained_backup`/`cleanup_error`) rather than being conflated with publish failure. `unique_backup`'s PID+atomic-serial naming makes collisions across processes/threads structurally impossible rather than relying on a check-then-use race window, and it's bounded (1024 attempts, then a typed error) rather than looping forever. No bug found. |
| 37 | Reparse-point-safe recursive delete — verified the underlying assumption | `crates/agenterm-platform/src/filesystem_cleanup.rs`, `adapters/windows/filesystem_cleanup.rs` (`remove_tree`) | **Audited, no bug (verified empirically, avoided a near-duplicate)** | Went looking for a Windows-junction analog of the existing Unix symlink-safety test after noticing the doc comment claims "does not intentionally follow ... Windows reparse points" but I'd only spotted a readonly-clearing Windows test on first read. Nearly wrote a new `mklink /J`-based regression test — then found `does_not_traverse_directory_reparse_points` already exists further down the same file and covers it more thoroughly than my draft would have (junction as the cleanup root, junction nested inside a larger tree, *and* an assertion that the junction target's permissions are untouched, not just its content). Independently useful: I'd already compiled and ran a standalone probe confirming `std::fs::remove_dir_all` itself (Rust 1.97.0, this host) stops at a real junction without recursing into it during its own internal traversal — which is the assumption this crate's Windows `remove_tree` implementation relies on for the nested case (it only special-cases reparse points it encounters directly via `read_dir`, not ones `remove_dir_all` might walk into on its own). Confirms the existing test's pass is backed by real interpreter behavior, not an untested assumption. No code change; the near-miss is the finding worth recording so a future pass doesn't repeat the same partial read. |
| 38 | Root reparse-point-classification primitive | `crates/agenterm-platform/src/filesystem_entry.rs`, `adapters/windows/filesystem_entry.rs` (`metadata_is_link_like`) | **Audited, no bug (verified empirically)** | Traced the dependency chain one level deeper than items 36/37: both `filesystem_publish.rs` and `filesystem_cleanup.rs` (and everything built on `metadata_is_real_directory`) ultimately trust this one Windows primitive to correctly identify reparse points. Its whole implementation is `metadata.file_attributes() & FILE_ATTRIBUTE_REPARSE_POINT != 0` against `0x0000_0400` — the correct real Win32 bit, and since Rust's `symlink_metadata` never follows the final link, the attribute bit reflects the link itself, not its target. Found this already has direct unit-test coverage of its own, not just indirect coverage via `filesystem_cleanup.rs`: `directory_junction_is_link_like_not_a_real_directory` creates a real `mklink /J` junction and checks classification two ways — via `symlink_metadata` and via an already-open handle obtained with `FILE_FLAG_OPEN_REPARSE_POINT\|FILE_FLAG_BACKUP_SEMANTICS` (so the open itself doesn't transparently follow the junction either). Ran both `filesystem_entry::tests::*` on this host with `cargo test -p agenterm-platform --all-features` — both pass. Side note for future audit passes on this crate: `cargo test -p agenterm-platform` with no flags only runs 10 tests (most modules are feature-gated and off by default) — `--all-features` is required to actually exercise `filesystem_entry`/`filesystem_cleanup`/etc.; not a bug, just easy to accidentally under-test this crate if you forget the flag. No bug found; closes out the filesystem-safety chain started at items 36/37. |
| 39 | Cross-process path/slot locking primitive | `crates/agenterm-platform/src/locking.rs`, `adapters/windows/locking.rs`, `adapters/unix/locking.rs` (`PathLock`, `SlotPermit`) | **Audited, no bug (verified empirically)** | Not yet covered this session despite being exactly the class of primitive (Win32 named-mutex slot semaphore) flagged as worth checking. Windows `PathLock`/`SlotPermit` both go through `acquire_named`, which treats `WAIT_ABANDONED` as a successful acquisition (correct recovery from a crashed prior holder) and correctly orders cleanup on a mid-loop failure (releases only the handles/local reservations actually acquired so far, in the right order). The interesting design point: Windows keeps a process-local `HashSet<String>` (`local_reservations`) that `PathLock`/`SlotPermit` must reserve *before* touching the kernel object — this isn't redundant bookkeeping, it's load-bearing. Win32 named mutexes track ownership per-*thread*, not per-handle, and are recursively reacquirable: a thread that already owns `Local\AgentermPlatform-X` succeeds immediately on a second `CreateMutexW`+`WaitForSingleObject` against a *new* handle to the same name, so without the local guard, two independent same-thread `PathLock` requests for the same path would both silently "succeed" instead of contending. The Unix implementation (shared by macOS via `#[path]`) needs no such guard, and for a non-obvious reason worth recording: it never canonicalizes or hashes the path at all — it just `open()`s the literal path and `flock()`s the resulting fd. Because the kernel resolves `..`/symlink components as part of `open()` and `flock()` locks are scoped to the resolved inode + open-file-description (not the path string), two different spellings of the same file (including a same-process double-open) naturally contend without any userspace alias detection — which is why `unix_directory_aliases_share_one_lock_identity` passes with zero canonicalization logic in that file, unlike Windows' `lock_identities`/`normalized_path`/`file_identity` machinery which exists specifically to replicate that same guarantee against a named-object namespace that has no such built-in aliasing. Ran the full `locking` test module (9 tests: path/hard-link/unicode-case alias detection, slot exhaustion+release, `..`-escape-past-root guard, UNC/extended-prefix normalization) on this host with `--all-features` — all pass. No bug found. |

| 40 | File-identity primitive underlying item 39's hard-link alias detection | `crates/agenterm-platform/src/file_identity.rs`, `adapters/windows/file_identity.rs`, `adapters/unix/file_identity.rs` | **Audited, no bug (one documented platform caveat noted)** | Followed item 39's `PathLock` hard-link-alias detection down to the primitive it calls (`crate::file_identity::path_identity`). Unix uses `st_dev`+`st_ino`, the textbook-correct stable POSIX object identity for the inode's lifetime — no issue. Windows uses `GetFileInformationByHandle`'s `dwVolumeSerialNumber`+`nFileIndexHigh/Low`, opened via `path_identity` with `FILE_FLAG_BACKUP_SEMANTICS` (needed to open directories) and full sharing flags; `same_object` compares both fields, matching the neutral facade's documented "stable across rename and hard-link aliases while the object exists" contract, confirmed by the existing `identity_survives_hard_link_and_rename_aliases` test (open-handle vs. by-path identity equality, hard-link convergence, rename survival) — ran it on this host, passes. One caveat worth recording rather than fixing: Microsoft's own docs note the file-index pair is not guaranteed unique on FAT/FAT32 (and can behave unexpectedly on ReFS with certain configurations), so on a FAT-formatted volume `PathLock`'s hard-link-alias detection could theoretically miss or misidentify aliases. Low real-world impact for its actual purpose — FAT/FAT32 don't support hard links at all, so the specific alias class this identity exists to catch can't occur there in the first place, and the separate lexical `path-` identity still provides the primary lock key regardless. Also checked `lock_identities`' TOCTOU window (path is canonicalized, then separately reopened for `path_identity`, so the target could theoretically be replaced in between): the neutral facade's own doc comment already discloses that `path_identity` isn't race-free and directs race-sensitive callers to the handle-based `file_identity` instead, so this is a documented, accepted limitation rather than an overlooked one. No bug found. |

| 41 | Detached-spawn creation-flags clobber on Windows | `crates/agenterm-platform/src/process_spawn.rs`, `adapters/windows/process_spawn.rs` (`configure_detached_command`, `configure_caller_job_fallback`) | **Fixed (doc only)** | `configure_detached_command`/`configure_caller_job_fallback` call `command.creation_flags(...)` on Windows, and `spawn_detached_child` calls the former then, on `ERROR_ACCESS_DENIED` from a job that denies breakaway, the latter on the *same* `Command` — relying on the second call's `CREATE_NO_WINDOW`-only flags overwriting the first's `CREATE_BREAKAWAY_FROM_JOB \| CREATE_NO_WINDOW`, which is the intended fallback behavior. Traced what `CommandExt::creation_flags` actually does on repeated calls, since std's own docs just say "sets" without stating replace-vs-accumulate — wrote and ran an empirical probe on this host (spawn with `CREATE_SUSPENDED` then immediately `.creation_flags(CREATE_NO_WINDOW)` again; if accumulate, the process stays suspended forever and never runs a trivial `copy NUL <path>`; if replace, it runs normally) — confirmed **replace**, not OR-accumulate. That's exactly what the fallback path needs, but it also means: if a *caller* had already set their own `creation_flags` on the `Command` before passing it to `configure_detached_command`/`spawn_detached_child`, those flags would be silently discarded with no error, no warning. Swept every `.creation_flags(` call site in the repo — the only other one is in the unrelated `research/agenterm-net` crate, which spawns via raw `command.spawn()` and never touches this API, so no live caller is affected today. Rather than guess at merge semantics callers might want (OR the bits? reject a pre-configured `Command`?), added a doc comment on `configure_detached_command` stating the replace behavior and requiring callers to pass an otherwise-unconfigured `Command` — a precise, low-blast-radius fix that closes the "nobody would know to avoid this" gap without changing behavior. `cargo check -p agenterm-platform --all-features` clean. |

| 42 | Named shared-memory create/open lifecycle | `crates/agenterm-platform/src/shared_memory.rs`, `adapters/windows/shared_memory.rs`, `adapters/unix/shared_memory.rs` | **Audited, no bug (verified empirically)** | Checked specifically for the item 27/28 bug class (an intervening Win32/libc call clobbering `GetLastError()`/`errno` before it's read) in a file that hadn't been looked at yet this session and has plenty of native error-checking surface: `CreateFileMappingW`'s `ERROR_ALREADY_EXISTS` check, `MapViewOfFile`/`mmap` failure paths that both clean up (`CloseHandle`/`close`+`shm_unlink`) *after* the error object is already captured, not before — every site reads the native error immediately after its triggering call with no intervening native call in between. Create-exclusivity is correctly enforced on both platforms (Windows relies on `CreateFileMappingW`'s documented "opens the existing object and sets last-error to `ERROR_ALREADY_EXISTS`" behavior rather than failing outright, closes the unwanted handle and reports `AlreadyExists`; Unix uses `O_EXCL` on `shm_open`, translating `EEXIST` the same way). `open()`'s undersized-mapping rejection is asymmetric by design and already documented in the existing test (Unix proactively `fstat`s and rejects with `SizeMismatch` before mapping; Windows has no pre-check and instead lets `MapViewOfFile` itself fail with `Map` when asked to map more than the section's committed size) — not a bug, the test asserts both outcomes explicitly per platform. `Drop` unconditionally unmaps/closes/unlinks and only `shm_unlink`s when this instance was the creator, matching the doc comment's "POSIX removes the name on creator drop; Windows keeps it until the last handle closes" contract. Ran all 3 `shared_memory::tests::*` (name/length validation, exclusive-creation-plus-shared-byte-view, oversized-open-rejection) on this host — all pass. No bug found. |

| 43 | Process principal/sandbox identity (token/SID) query | `crates/agenterm-platform/src/process_security.rs`, `adapters/windows/process_security.rs`, `adapters/linux/process_security.rs`, `adapters/macos/process_security.rs` | **Audited, no bug (verified empirically)** | Focused on the Windows adapter, the most bug-prone part: `token_information`'s classic two-call `GetTokenInformation` sizing idiom deliberately ignores the first call's return value (expected to fail with `ERROR_INSUFFICIENT_BUFFER` — this is the documented correct pattern, not a missed check), allocates a `Vec<usize>` (not `Vec<u8>`) sized via `div_ceil` specifically so the raw buffer comes out pointer-aligned for the struct that gets cast over it, and correctly re-checks the second call's return value with no intervening native call before reading `last_os_error()` (the item 27/28 bug class, again absent here). `copy_sid` goes further than a naive implementation would: after `IsValidSid`, it explicitly bounds-checks that the `TOKEN_USER`/`TOKEN_APPCONTAINER_INFORMATION` struct's embedded SID pointer actually falls within the buffer just allocated (`checked_add`-guarded start/end comparison) before treating it as a valid byte range to copy — defense against a malformed/unexpected buffer layout rather than blindly trusting an OS-supplied offset pointer. The `required < minimum_size` sanity gate after the first sizing call correctly surfaces the *real* underlying error (e.g. a genuine `OpenProcessToken`/handle-access failure) rather than a misleading buffer-size message, since Windows only writes a nonzero `ReturnLength` on the insufficient-buffer failure path specifically — `required` stays at its Rust-side zero-initialization for other failure kinds, and `minimum_size` catches that. Linux's `/proc/[pid]/status` parser correctly reads the second whitespace-separated column of the `Uid:`/`Gid:` lines (real, effective, saved-set, filesystem, per `proc(5)`) as the effective ID, matching its test. macOS's `libproc`-based reader maps `pbi_uid`/`pbi_gid` to effective IDs (`pbi_ruid`/`pbi_svuid` would be real/saved) — consistent with `proc_bsdinfo`'s established field-naming convention, though not independently cross-checked against Apple's own header on this Windows host. Ran all 5 `process_security` tests on this host — all pass. No bug found. |

| 44 | Suspended AppContainer process creation — pointer lifetime and fail-closed semantics | `crates/agenterm-platform/src/adapters/app_container_process.rs`, `adapters/windows/app_container_process.rs` | **Audited, no bug (verified empirically)** | The most native-interop-dense file reviewed this session: builds a `SECURITY_CAPABILITIES` struct plus a `PROC_THREAD_ATTRIBUTE_LIST` and calls `CreateProcessW` with `CREATE_SUSPENDED` to sandbox a process before it ever runs caller code. Traced every raw pointer's lifetime: `security_capabilities.AppContainerSid`/`.Capabilities` point into locals (`app_container_sid`, `capabilities: Vec<SID_AND_ATTRIBUTES>`) that outlive the `CreateProcessW` call and are never reallocated after those pointers are taken; `capabilities`' own `Sid` fields point into `capability_sids: Vec<_>` elements, safe because moving the *outer* Vec during growth relocates only each inner aligned-buffer's Vec header, never the heap data the pointer targets. `AttributeList` holds `raw: LPPROC_THREAD_ATTRIBUTE_LIST` pointing into its own `_storage: Vec<usize>` field — same heap-indirection argument keeps it valid across moves, and struct-field drop order (custom `Drop` body runs before auto-generated field drops) means `DeleteProcThreadAttributeList` always runs before `_storage`'s backing memory is freed. Handle-inheritance allowlisting is done correctly: `bInheritHandles` is only `TRUE` when an explicit handle list is non-empty, and `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` then restricts real inheritance to exactly that list regardless of what else in the process happens to be inheritable-flagged — the documented, correct idiom, not a naive "just pass bInheritHandles=TRUE" approach. Error-path correctness: the `GetLastError()` capture for a failed `CreateProcessW` happens inside the same closure passed to `with_inheritable_handles`, before that helper's own restore-phase native calls run, so item 27's ordering bug doesn't recur here; and because the closure captures `information` (`PROCESS_INFORMATION`) by mutable reference rather than by value, a successful `CreateProcessW` followed by a *later* failure in the wrapping helper's handle-flag restore step still leaves the real process handle visible to `cleanup_information`, so a live suspended process can't leak even on that specific error interleaving. `SuspendedAppContainerProcess::resume`/`Drop` are fail-closed exactly as documented — confirmed both the direct-drop-without-resume path and (by inspection of `?`-early-return semantics) the resume-failure-triggers-drop path terminate and wait for the exact process object rather than leaving an orphaned suspended process. Ran the full `app_container_process` test module on this host, including the real-process tests that create an actual AppContainer profile, spawn a suspended `hostname.exe` inside it, and either resume-and-wait or drop-and-verify-termination — all 5 pass. No bug found. |

| 45 | REPL history navigation desyncs after an empty submit mid-navigation | `crates/agenterm-platform/src/console_line_editor.rs` (`LineHistory::push`) | **Fixed** | Pivoted to freshly-landed code (`693172b`, the new per-key REPL line editor) rather than re-auditing already-well-covered native code. `LineHistory::push`'s three branches were meant to be symmetric — every submission ends the current navigation session — but only two of the three called `self.reset()`: the duplicate-collapse branch and the normal-append branch did, the empty-line early return did not. Reachable in practice: press Up to recall a history entry (`position` becomes 1, `draft` saved), then clear the recalled text back to empty (backspace/Ctrl+U) and press Enter — `render_edit_line`'s caller in `src/client/mod.rs::run_repl_line_editor` calls `buffer.take()` then unconditionally `history.push(&line)` on every Enter, including an empty one. With `position` stuck at 1, the *next* Up press on a fresh input line skips the `position == 0` branch that saves the new draft, and returns `entries[len - 2]` instead of the most recent entry — pressing Up once jumps straight to the *second*-most-recent history item, silently skipping the top of history, and the draft that should have been restorable on the way back down is never captured. Wrote a regression test reproducing the exact sequence (push two entries, `previous()` once, `push("")`, then verify a *fresh* buffer's `previous()` still returns the most recent entry) — confirmed it fails against the old code (`Some("first")` instead of `Some("second")`) before fixing. Fix: the empty-line branch now calls `self.reset()` before returning, matching the other two branches. `cargo test`/`clippy -D warnings` clean, all 13 `console_line_editor` tests (up from 12) pass. |

| 46 | Windows console line editor drops repeats coalesced into one `wRepeatCount` record | `crates/agenterm-platform/src/adapters/windows/console_line_editor.rs` (`Editor::read_key`) | **Fixed (verified empirically)** | `read_key()` reads exactly one `INPUT_RECORD` per call and decodes it via `map_key`, but never read `KEY_EVENT_RECORD::wRepeatCount`. MSDN documents that a held key can be reported as one record with `wRepeatCount` greater than one instead of one record per press ("you might get five events with this member equal to 1, one event with this member equal to 5, or..."), and console-input coalescing under load is a real, not just theoretical, Win32 behavior. Verified directly on this host rather than trusting the docs alone: wrote a throwaway probe using `WriteConsoleInputW` to inject a synthetic `KEY_EVENT_RECORD` with `wRepeatCount=3` into a real console input buffer (via `AllocConsole` + `CreateFileW("CONIN$", ...)`, since `cargo test`'s stdin isn't a console), then read it back with `ReadConsoleInputW` and confirmed `wRepeatCount` round-trips intact as `3` — proving the scenario this code needed to handle is real, not hypothetical, then discarded the probe (not something to keep as a permanent test; it mutates process-wide console state and needs `AllocConsole`, unlike the codebase's other console tests). Fixed by adding `pending_key`/`pending_repeat` fields to `Editor`: on decoding a record, queue `wRepeatCount - 1` additional replays of the same key instead of discarding them, and drain the queue at the top of `read_key()` before touching the console again. Added `held_key_repeat_count_is_replayed_not_dropped`, which exercises the queue-drain logic directly (constructing an `Editor` with null handles, since the queued-replay path never touches them) and confirms the *next* call after the queue drains falls through to a real console read rather than replaying a third time. `cargo test`/`clippy -D warnings` clean, `console_line_editor` module now 14 tests (was 13). |

| 47 | Unix console line editor drops every key but the last from a multi-key `read()` | `crates/agenterm-platform/src/adapters/unix/console_line_editor.rs` (`Editor::read_key`) | **Fixed** | More severe than item 46's Windows-side sibling, and reachable under ordinary use rather than only under load: `read_key()`'s contract is one key per call, but `self.parser.feed(&bytes[..read])` can decode *multiple* keys from a single `read()` — normal whenever more than one typed byte is already buffered when the poll loop wakes (fast typing, or pasting text, which is exactly the common case of delivering many bytes in one read) — and the code did `keys.pop()`, which returns only the *last* `ConsoleKey` and silently drops every earlier one in that batch. Concretely: pasting `"abc"` into the REPL would insert only `'c'`; typing two characters faster than the ~10ms poll window could lose the first. The existing `EscapeParser::feed` tests already prove the parser itself correctly returns multiple keys per call (e.g. `decodes_ascii_and_control_bytes` feeds `b"ab\r\n\x7f"` and gets 5 keys back) — the bug was entirely in the adapter discarding all but one of them, not in the shared parser. Fixed the same way as item 46: added a `pending: VecDeque<ConsoleKey>` queue to `Editor`, draining it (FIFO, oldest first) at the top of `read_key()` before touching `poll`/`read` again, and pushing every key `feed()` returns onto it instead of only keeping the last. Couldn't run this file's tests on this Windows host directly (it only compiles under `adapters/linux`/`adapters/macos`'s `#[path]` re-export, itself gated to `cfg(target_os)`), so cross-compiled and cross-clippied for `x86_64-unknown-linux-gnu` (`cargo check`/`clippy -D warnings --tests`, both clean) and added `keys_decoded_from_one_read_are_all_replayed_in_order`, which constructs an `Editor` with a pre-filled queue (bypassing the untestable real-termios path, same technique as item 46) and confirms three queued keys come back in order across three calls. Full 239-test `agenterm-platform` suite still passes on this Windows host (this file isn't part of it, but confirms no collateral regression). |

| 48 | Close out the REPL line-editor feature audit (items 45-47) | `crates/agenterm-platform/src/contract/console_line_editor.rs`, `src/client/mod.rs` (`render_edit_line`, `run_repl_line_editor`'s command/interrupt coordination, `run_repl_plain_reader`) | **Audited, no further bug** | After landing 3 real fixes in this freshly-shipped feature (45: history navigation desync, 46: Windows repeat-count drop, 47: Unix multi-key-per-read drop), swept the rest of the surface before moving on. `contract/console_line_editor.rs`'s `ConsoleLineEditorError`/`ConsoleKey` types are plain, correctly exhaustive-per-variant data carriers, no issues. `render_edit_line`'s cursor-repositioning math (`\x1b[{suffix_columns}D` computed from `unicode_width::UnicodeWidthChar::width` summed over the post-cursor suffix) correctly handles wide (CJK) characters, and its `filter_map` silently-zero-contribution for `width() == None` characters is a dead branch in practice since `LineBuffer` can never contain a raw ASCII control byte (every adapter's key mapping routes control bytes to a dedicated `ConsoleKey` variant before they could ever become a `Char`, never inserting them into the text buffer directly) — not a bug, just unreachable defense. `run_repl_line_editor`'s `ReplEditorCommand`/`ReplInterruptEvent` coordination checks `watcher.events` at three separate points (before reading input, immediately after `session.inspect()`, and after evaluation) rather than once, which is deliberately defensive against a Ctrl-C arriving mid-operation rather than a race; `run_repl_plain_reader`'s `Clear` command is correctly a no-op there since the plain (non-tty) reader has no in-progress edit buffer for it to discard. No further bug found in this feature area; future passes should look elsewhere first. |

| 49 | Cockpit diagnostic lines corrupted by control characters in tab titles/notes | `src/control_center.rs` (`connected_cockpit_lines`, new `cockpit_display_field` helper) | **Fixed (verified empirically)** | Traced a lead from the fresh Cockpit tab-row feature (item 45-48's neighbor, commit `2ff9dbd`) that turned out to implicate older code too: `tab.title`/`tab.note` are set entirely via `rename-window`'s free-form CLI argument (`src/control_dispatch.rs`'s `"renamew"` handler, `host.tabs_mut()[position].title = name.to_owned()`), and `UiBootstrapSnapshot::validate()` only enforces a byte-length cap (`UI_TAB_TITLE_MAX_BYTES`/`UI_TAB_NOTE_MAX_BYTES`) — nothing anywhere rejects embedded control characters. `connected_cockpit_lines` interpolates both fields raw into `format!`, assuming each `Vec<String>` entry renders as exactly one terminal row — a title containing `\n` or a raw ANSI escape breaks that invariant. Initially suspected this could be untrusted-PTY-program-controlled (like OSC7 CWD reporting, item 35) via OSC title-setting, but traced `terminal_runtime.rs`'s `set_window_title` far enough to confirm `tab.title` is a separate, purely-user-set field from the OSC-set `parser.callbacks().title` (kept deliberately apart, exposed separately as `terminal_title` elsewhere) — so this is self-inflicted (a user or automation renaming their own tab), not a cross-tab/cross-process attack, which scopes it as a robustness/rendering-corruption fix rather than a security boundary fix. Wrote a regression test with a title containing `\n`+ESC and a note containing `\r\n`, confirmed it fails against the code with only the pre-existing `active`-line interpolation reverted (`"Active      @1 · evil\ntitle\u{1b}[31m"` observed raw in the panic output — real, not hypothetical), then fixed by adding `cockpit_display_field`, which replaces any `char::is_control` character with a space, and applying it at all three interpolation sites (`active`, per-tab `title`, per-tab `note`). `cargo test`/`clippy -D warnings` clean, full `control_center` module (42 tests, +1) and both cockpit-lines tests pass. |

| 50 | Sweep for more instances of item 49's bug class | `src/mcp_fleet.rs`, `src/platform/adapters/unix/frontend/mod.rs` (`active_cwd_status_text`), tab-bar rendering | **Swept, no further instance found** | Checked whether item 49's pattern (free-form user/PTY-controlled string interpolated raw into an assumed-single-line text output) repeats elsewhere. `mcp_fleet.rs`'s tab `"note"` field passes through as a `serde_json::Value` (`"note": tab["note"]`), not string-interpolated into plain text — JSON encoding already escapes control characters (`\n` → `\\n`, etc.), so it isn't the same bug class. `active_cwd_status_text`'s CWD text comes from the OSC7 pipeline already hardened in item 35 (control bytes rejected during `percent_decode`/`validate_path`), so no fresh exposure there. Did not find the actual tab-bar (as opposed to Cockpit-diagnostic) title-drawing code path in this pass — it renders through the app's own cell-based UI painter rather than raw stdout, which is a structurally different (and likely safer, since embedded ESC bytes drawn into a cell grid aren't re-interpreted as terminal control codes the way a raw `println!` would be) exposure than what item 49 fixed; flagged here rather than chased further this iteration, since finding and auditing that renderer is a separate, larger task. |

| 51 | Follow up on item 50's flagged gap: does the real tab-bar renderer share item 49's bug? | `src/platform/adapters/unix/frontend/mod.rs` (`sidebar_rows`), `render.rs` (`render_sidebar`, `tab_row_title`, `draw_text`) | **Audited, no bug** | Closed the gap item 50 explicitly left open. AgenTerm has no top-of-window tab strip — the per-tab UI is a sidebar tree (`SidebarTabRow`), built straight from `tab.title.clone()` with no sanitization, same as the pre-fix Cockpit code. But the rendering path is structurally different from what item 49 fixed: `tab_row_title` truncates by Unicode `char` count using a width-aware budget (`geometry.name.width() / (GLYPH_WIDTH + 1)`) before `draw_text` ever runs, and `draw_text` iterates `text.chars()` (never raw bytes) through either a proportional glyph lookup or a bitmap font covering only a printable-ASCII range — a control character like `\n` or ESC falls outside that range, hits the fallback branch, advances the cursor by one glyph width, and draws nothing. Critically, this path is a plain glyph blitter clipped to a pixel buffer/rect, never routed through `vt100::Parser` or any ANSI/OSC interpreter, so an embedded ESC byte can't trigger escape-sequence side effects the way it could in a `println!`-rendered diagnostic line — there's no interpreter present to exploit. One unrelated, pre-existing, non-security cosmetic quirk noted in passing: `max_chars` assumes a fixed per-glyph advance, so a title full of variable-width (CJK/emoji) characters could visually overrun the sidebar column before the pixel-level clip catches it — not something newlines/ESC bytes specifically cause, not pursued. Windows' native GDI-based tab UI (`control_window.rs`, already heavily audited as items 5/5b) draws via `TextOutW`-family APIs with the same "dumb glyph blitter, no ANSI interpreter" property, so the same reasoning is expected to generalize there, though not independently re-verified line-by-line this pass. No fix needed. |

| 52 | Screenshot capture and PNG encoding | `crates/agenterm-platform/src/screenshot.rs`, `adapters/windows/ui_screenshot.rs` | **Audited, no bug** | Checked the XRGB→PNG encoding path (`checked_pixel_count`/`checked_clip` overflow-safe bounds checking, correct flat-buffer row/column indexing bounded by the already-validated frame dimensions) and the Windows native `BitBlt`/`GetDIBits` GDI capture path. The GDI path is carefully sequenced: `rgba_buffer_len` rejects negative or oversized dimensions (via `usize::try_from` on the signed GDI rect deltas) *before* `CreateCompatibleBitmap` ever sees them; `BitBlt`'s and `GetDIBits`' return values are both checked before use, with `GetDIBits` skipped entirely (`scanlines = 0`) if `BitBlt` already failed; `SelectObject(memory_dc, previous)` deselects the bitmap from the DC *before* `Bitmap`'s `Drop` calls `DeleteObject` on it (avoiding the classic GDI "delete a selected object" bug), and Rust's reverse-declaration-order drop sequence (`bitmap` → `memory_dc` → `source`) matches the required GDI teardown order without needing an explicit `Drop` ordering trick. `validate_client_area` correctly rejects a negative clip origin/size via the same `TryFrom`-failure-as-rejection pattern. Boundary tests exist and pass for the exact max-pixel-count case (`8192×4096 == MAX_CAPTURE_PIXELS` accepted, `+1` column rejected). No bug found; this and the platform-crate native-interop files audited so far this session (locking, shared_memory, process_security, app_container_process, this one) are consistently well-defended — the fresher `src/`-level product code has been the more productive hunting ground this session (items 45-49 all landed there), so future passes should weight toward recently-changed `src/` files over re-auditing `agenterm-platform`'s already-hardened native adapters. |

| 53 | Unix `close_settings` drops font-family changes on apply | `src/platform/adapters/unix/frontend/mod.rs` (`close_settings`) | **Fixed (currently unreachable, defensive)** | Went back to `a697830` ("centralize settings modal semantics for both frontends"), the commit that introduced the shared `SettingsDialog` (`src/frontend/settings.rs`) — exactly the kind of state-management refactor where item 45's history-navigation bug lived, so worth a second pass on its sibling refactors. Found an asymmetry: Windows' apply path (`remote_frontend.rs:3814`) copies all three fields back — `next.terminal_font_family = changes.default_appearance.terminal_font_family.clone(); next.terminal_font_size = ...; next.color_theme = ...;` — plus per-tab `override_draft`/`target_tab_id` handling. Unix's `close_settings` only copied `terminal_font_size` and `color_theme`, silently dropping any font-family change and never touching per-tab overrides at all. Traced whether this is live today: Unix's `open_settings()` always passes `target_tab_id: None`, and nothing in the Unix frontend calls `switch_scope`/`SettingsScope::CurrentTerminal` or `set_font_family_draft` (confirmed via a repo-wide grep — only `remote_frontend.rs:3661` calls `set_font_family_draft`), so per-tab overrides and font-family editing are Windows-only features today; `capture()`'s `self.default_draft.terminal_font_family = family` on Unix just reassigns the unchanged value back to itself. Not a live bug, but a real trap for whoever wires up font-family editing on Unix next (a natural, near-term extension given the whole point of this refactor was a *shared* dialog). Added the missing `self.config.terminal_font_family = changes.default_appearance.terminal_font_family;` line, matching the Windows pattern exactly (verified matching `String`/`String` field types by reading both `AppConfig`/`EffectiveTerminalAppearance` struct definitions directly). Could not fully type-check this specific edit on this Windows host: cross-compiling the *main* `agenterm` crate (unlike the smaller `agenterm-platform` crate used for items 46/47) fails here because a transitive dependency's build script needs a Linux C cross-compiler this host doesn't have, and the file itself is `cfg`'d out of Windows builds entirely. Verified what's checkable without that: `cargo fmt --check` confirms valid Rust syntax and formatting. Left the per-tab `override_draft`/`target_tab_id` gap in `close_settings` unfixed — that's a genuinely bigger feature-completeness question (does Unix want the whole per-tab-override UI, not just this one field) rather than a narrow, safe copy-paste fix, so it's a "needs a decision" note rather than something to fix unilaterally. |

| 54 | Windows new-terminal dialog closes even when IPC create fails, losing the old retry UX | `src/platform/adapters/windows/remote_frontend.rs` (`finish_new_terminal`), `src/frontend/new_terminal.rs` (`NewTerminalDialog::finish`) | **Found, needs a design decision** | Continued auditing the sibling refactor to item 53 (`dc6fa9b`, "align Windows new-terminal state with shared dialog"). Diffed the old vs. new `finish_new_terminal`: the *old* code, on the IPC "new-window" send failing (`result.is_err()`), set `last_error` and `return`ed *before* reaching `self.new_terminal_open = false` — keeping the dialog open with the user's shell choice / initial command / proxy drafts intact so they could see the error and retry without re-entering everything. The *new* code sets `last_error` on the same failure but has no `return`, falling through to `show_new_terminal_controls(false)` / `show_workspace_controls(true)` / `layout()` — closing the dialog regardless. Root cause: the shared `NewTerminalDialog::finish(true)` (in `new_terminal.rs`) already calls `close_without_create()` internally as part of returning `Ok(Some(CreateParams))` on *successful validation* — before the caller has even attempted the downstream action (the IPC send on Windows, or `create_tab` on Unix) that could still fail. Checked whether this is a genuine Windows-only regression or matches Unix's longstanding behavior: Unix's `finish_new_terminal_dialog` calls `create_tab` directly (no separate IPC step) and has *always* closed the dialog regardless of `create_tab`'s success (its `Ok(Some(params))` arm has no error-handling branch at all if `create_tab` fails, silently swallowing that specific failure) — so Unix never had the "stay open on failure" behavior to begin with. This means the refactor didn't introduce inconsistency between the two platforms so much as it dropped a real, useful Windows-specific safeguard by coupling both platforms to the same eager-close contract. Not fixed: the correct fix isn't a narrow one-liner like item 53's — it requires deciding whether `finish()`'s contract should change (close only after the caller confirms the downstream action succeeded, which would also let Unix start reporting `create_tab` failures instead of swallowing them) or whether Windows should reconstruct/reopen the dialog with preserved drafts specifically on IPC failure without touching the shared contract. Recording for a deliberate decision rather than rushing an architectural change into an already 150+-line refactor diff. |

| 55 | Unix mouse-wheel-in-full-screen-app fallback missing `application_cursor`, mirroring a bug already fixed on Windows | `src/platform/adapters/unix/frontend/mod.rs` (`mouse_wheel`) | **Fixed (verified via a proven twin fix + primitive test)** | `65d7f40` ("...fix full-screen wheel fallback") already fixed this exact bug on Windows: the gate for converting a mouse-wheel scroll into arrow-key bytes (via `alternate_screen_wheel_bytes`) checked only `alternate_screen`, missing TUI apps that enable `application_cursor` (DECCKM) mode without switching to the alternate screen buffer — such apps would get their scrollback silently scrolled by the wheel instead of receiving arrow-key input, since the wheel handler never falls back to sending bytes to the app. Windows' fix introduced `full_screen_input = alternate_screen || application_cursor` and applied it consistently at both of its two gate sites. Checked whether Unix ever received the same fix: it never did — `mouse_wheel`'s single fallback site still reads `} else if alternate_screen {`, even though `application_cursor` is already destructured into scope right next to it and gets passed to `alternate_screen_wheel_bytes` on the very next line (so the correct escape-sequence *selection* logic was already there, only the *gate* was stale). Confirmed `alternate_screen_wheel_bytes` itself correctly branches on `application_cursor` to choose `\x1bOA`/`\x1bOB` (DECCKM application mode) vs. `\x1b[A`/`\x1b[B` (normal mode) — the primitive was always ready for this, only the caller's condition wasn't updated. Fixed by widening the gate to `alternate_screen \|\| application_cursor`, mirroring the Windows fix exactly. Couldn't run this Unix-only file's own tests on this Windows host (same cross-compile constraint as item 53 — no Linux C toolchain here, and `mouse_wheel` has no dedicated unit test requiring a full `UnixApp` fixture regardless), so verified via `cargo fmt --check` (syntax/formatting clean) and by re-running `alternate_screen_wheel_respects_cursor_mode_and_bounds_input` (the underlying, cross-platform-testable primitive `alternate_screen_wheel_bytes` depends on) — passes, confirming the primitive this fix now reaches is correct. |

| 56 | IME text-commit control-character filtering | `src/frontend/input.rs` (`normalize_ime_commit`), Unix `commit_ime_text` call sites | **Audited, no bug (one open question noted)** | Checked the last sibling of the frontend-refactor series (`d24a2a4`, "move keyboard input policy out of Unix adapter") for the same class of gap items 53/55 found: `normalize_ime_commit(text, multiline)` filters control characters and DEL, letting `\n`/`\r` through (normalized to `\n`) only when `multiline` is true. Its four Unix call sites choose `multiline` correctly per field: the new-terminal dialog's "initial command" field allows it (a shell command can legitimately be multi-line), its HTTP/HTTPS proxy fields and the tab-editor's non-note fields don't (single-line by nature), and — notably — the direct-to-terminal path also uses `multiline=false`, meaning a stray `\n`/`\r` that somehow rode along in an IME commit gets silently dropped rather than forwarded raw to the PTY; real Enter keypresses arrive through a separate dedicated key-handling path, not IME commits, so this is a deliberate safety margin, not a functional gap. The existing "empty IME commits happen on bare CN/EN toggles and must not clear a selection" guard is already handled and commented. One open question, not chased further this pass: `normalize_ime_commit` isn't called anywhere in `remote_frontend.rs`, and no Windows-side IME-commit filtering function was found either — this refactor's commit title says it moved the logic *out of the Unix adapter* (not "centralized for both frontends," unlike the settings/new-terminal refactors), so this may be intentional (Windows' native IME integration could commit text through a different path that doesn't need the same filtering) rather than a gap; confirming which would need dedicated IME-architecture research rather than a quick check, so left as a note for a future pass instead of a guess. |

| 57 | Verify the cockpit key-navigation fix is complete | `src/control_center.rs` (`classify_cockpit_input`, `cockpit_presentation`) | **Verified complete, no further bug** | Traced `635207f`'s already-shipped fix (keyboard Up/Down/Home/End/Enter previously indexed into the 3-row-truncated view instead of the full tab list) rather than assuming a "fix" commit needs no second look. Confirmed `ordered_tab_ids` (built in `cockpit_presentation` from the same `server.tabs` reference) and the independently-recomputed `selected_index` fallback stay index-compatible because both callers share one unmutated `server` snapshot between the two calls — no divergence window. The `.get(index).or_else(first/last).expect(...)` chain is defensive redundancy on top of already-clamped arithmetic (`saturating_sub`/`saturating_add().min(...)`), not masking a real gap, since the function's own `!server.tabs.is_empty()` guard at the top already rules out the only case that `.expect` would panic on. All 42 `control_center` tests (including this fix's own coverage) pass. No further bug found. |

| 58 | Recursive directory-tree ACL grant: TOCTOU symlink-substitution safety | `crates/agenterm-platform/src/adapters/windows/directory_access.rs` (`grant_tree`, `grant_entry`), `filesystem_open.rs` (`open_existing_path_with_access`) | **Audited, no bug (verified the safety mechanism, not just assumed it)** | `grant_tree` recursively walks a directory tree granting ACL entries — exactly the shape of the classic `chown -R`/`chmod -R` symlink-race CVE class: it `symlink_metadata`s a path, checks `metadata_is_link_like`, then (if not link-like) calls `grant_entry`, which does a *separate* native open before mutating the DACL. Between the stat and the open there's a window where an attacker with write access to a parent directory could substitute a symlink pointing elsewhere, redirecting the ACL grant to an unintended target. Traced whether this window is actually exploitable: `grant_entry` opens via `open_existing_path_with_access`, whose own doc comment claims "race-resistant" — verified that's not just an aspirational comment by reading both layers. The abstraction (`filesystem_open.rs`) walks the path one component at a time using `open_existing_child`, anchored to an already-open *directory handle* rather than re-resolving path strings, so a later component can't be swapped out from under it; the final object's type is verified through the *same opened handle*, not a separate racy stat. Confirmed the native Windows layer actually delivers this: `adapters/windows/filesystem_open.rs`'s `open_existing`/`open_existing_child` pass `FILE_FLAG_OPEN_REPARSE_POINT` to `CreateFileW`, so a reparse point is opened as itself rather than transparently followed — the mechanism the whole safety argument depends on is real at the syscall level, not just at the Rust abstraction. `grant_tree`'s own early `metadata_is_link_like` check is a fast-path skip-and-count optimization layered on top of this real security boundary, not the boundary itself. Also checked `ACCESS_MODIFY_CONTENTS`'s bitmask (existing test already asserts it excludes `WRITE_DAC`/`WRITE_OWNER`, so granted access can't be used to further escalate permissions or reassign ownership) — correct. All 3 `directory_access` tests (including the junction-skip fixture) run and pass on this host. No bug found. |

| 59 | Cross-platform check: does Unix's `filesystem_open.rs` share the same TOCTOU-safety property verified for Windows in item 58? | `crates/agenterm-platform/src/adapters/unix/filesystem_open.rs` | **Audited, no bug (verified symmetric)** | `open_existing`/`open_existing_child` use `O_NOFOLLOW` (Unix's analog of item 58's `FILE_FLAG_OPEN_REPARSE_POINT`) plus `openat()` for the child-relative case — the same directory-handle-anchored design as Windows. Checked the one real subtlety with `O_NOFOLLOW`: POSIX only guarantees it rejects a symlink in the path's *trailing* component, not intermediate ones, so `O_NOFOLLOW` alone would be insufficient if any single `open()`/`openat()` call here were ever given a multi-component path. Confirmed that never happens: the Rust-level orchestration in `filesystem_open.rs` (shared, platform-neutral) walks the path one component at a time — the first `open_existing` call only ever receives the *root anchor* (a single-level path), and every subsequent component goes through `openat()` via `open_existing_child`, so every native call's "trailing component" is its only component. `O_NOFOLLOW`'s protection is airtight at every step, not just superficially applied. Cross-compile-checked (`cargo check -p agenterm-platform --all-features --target x86_64-unknown-linux-gnu --tests`) clean on this host. No bug found; closes out the cross-platform half of item 58's verification. |

| 60 | Shared CWD editor dialog state (`bd6ed2b`/`d18b106`) | `src/frontend/cwd_editor.rs`, its Windows/Unix call sites | **Audited, no bug** | Checked the last unreviewed shared-frontend module in the refactor family that produced items 53/55/56. `close_and_take_target()` is only called on Unix; Windows exclusively uses `close()`. Initially looked like a possible asymmetry (Unix needing the target post-close, Windows silently losing it), but traced Windows' `finish_cwd_editor` and confirmed it captures `self.cwd_editor_dialog.target()` at the *top* of the function, before eventually calling `close()` near the end — functionally identical outcome to Unix's combined call, just different code shape (grab-then-close vs. close-and-take). Windows' other two `close()` sites are unconditional modal-reset-on-reconnect paths that never needed the target in the first place. `submit_mode`'s shift-before-alt precedence when both modifiers are held is an arbitrary but harmless UX choice for an ambiguous chord, not a bug. No bug found. |

## Done

- #1 `8e1e413` — fork()/execve() async-signal-safety in the POSIX PTY adapter.
- #5 `20144a1` (refined by `7c7409e`/`7dead17`) — reentrant `window_proc` `&mut State` aliasing.
- #9 — Unix `close_tab_id` now keeps the closest surviving neighbor active, matching `server_app.rs`.
- #10 `524ac98` — Windows sidebar geometry delegates to the shared `ui_geometry` functions.

- #5b — closed after four audit passes over `control_window.rs` found nothing further.
- #11 — three inlined copies of scrollbar hit-testing converted to call the shared `scrollbar_hit_test`.
- #12 — 8 stale `#[allow(dead_code)]` markers removed from `ui_geometry.rs`.
- #15 — Linux clipboard helper-existence probe hardened against shell injection.
- #16 — instance-registry write/lock lifecycle audited, solid; the redundant pre-rename `remove_file` fixed after empirical confirmation, one observation still open.
- #26 `4a8b5d3` — fixed STILL_ACTIVE (259) exit-code ambiguity in Windows process liveness checks, with a real regression test that caught a bug in the first fix attempt.
- #27 `30650fd` — fixed a last-error capture ordering bug in Windows process metrics reading (wrong error attached, not a wrong outcome).
- #13 `ac14461` (other agent) — standing catalog-vs-registration regression guard, using a dev-dependency-only rhai `metadata` feature to sidestep the release-binary size-budget conflict.
- #31 `84ce5ff` — settings.json now written atomically (temp-then-rename), matching the pattern already used in instances.rs, closing a silent all-preferences-lost-on-crash risk.
- #32 `2964650` — workspace.json now written atomically too; the same crash-mid-write risk but for the entire tab tree. Swept the rest of the repo's `fs::write` sites and confirmed these were the only two real gaps.

- #8 `b6b9e5c` — capped concurrent stream-pump threads at 64 with a typed failure, implementing the option-A design proposed here (with the specific limit and typed-error-vs-backpressure choice both taken as recommended).

## Open

- #16's remaining observation: confirmed (not just suspected) missing symlink/ancestor hardening on the instances directory's rare shared-`/tmp` fallback path (Linux/macOS, `HOME`+`XDG_*` all unset) — reusable building blocks (`protect_private_directory`, `metadata_is_real_directory`) already exist in the codebase; needs a careful pass, not a rushed one.
- #22 — unbounded frame/invocation dedup `HashSet`s in the persistent worker; needs a decision on bounding strategy (and its replay/cancel-classification tradeoffs) before implementing.
