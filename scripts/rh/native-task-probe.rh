fn entry() {
    let first = args[0];
    let present = std::fs::exists(first);
    let total = args.len + first.len + present;
    for value in 1..5 {
        total += value;
    }
    total
}

fn cc_lines() {
    ["rh native named-task probe"]
}
