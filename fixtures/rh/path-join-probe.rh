// Probe native std::path::join lexical path composition.
fn entry() {
    if args.len != 1 {
        return rh::fail("expected: ROOT");
    }

    let joined = std::path::join(args[0], "child").display;
    joined.len
}
