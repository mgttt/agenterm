// Probe native std::fs::read_to_string for text file ingestion.
// Args: PATH
fn entry() {
    if args.len != 1 {
        return rh::fail("expected: PATH");
    }

    let text = std::fs::read_to_string(args[0]);
    text.len
}
