[package]
name = "agenterm-tinyvm"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
description = "Tiny stack bytecode VM: a portable, dependency-free interpreter that runs where JIT is forbidden or absent (iOS, wasm)"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
